#!/usr/bin/env dictu 

import System;

from "csv.du" import Opts, Parser;

// main
{
    const opts = Opts("data.csv", true);
    Parser(opts)
        .parse()
        .forEach(
            def(row) => print(row)
        );

    System.exit(0);
}
