import Random;
import System;

const characters = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*()-_=+,.?/:;{}[]~";
const wordsFile = "/usr/share/dict/words";

class Password {
    init(var pass) {
        this.len = pass.len();
    }
}

def generate(length) {
    var password = "";
    print(Random.range(0, characters.len()));
}

class Salt {
    init(var length) {}
}

{ // main
    const password = generate(8);
    print(password);

    System.exit(0);
}
